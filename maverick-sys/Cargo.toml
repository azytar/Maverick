[package]
name = "maverick-sys"
version = "0.18.1"
edition = "2021"
description = "Safe wrapper around the libc FFI Maverick needs (signals + daemon detach)"
license = "GPL-3.0"

[dependencies]
libc = "0.2"
