[package]
name = "maverick-img"
version = "0.1.0"
edition = "2021"
description = "Dependency-free image decode for Maverick's native wallpaper (PNG + trivial formats + external-converter fallback)."
license = "GPL-3.0"

[lib]
name = "maverick_img"
path = "src/lib.rs"

[dependencies]
